/**
 * @param {number} num
 * @return {boolean}
 */
 function isInt(num) {
 	return num -Math.floor(num) == 0;
 }
var checkPerfectNumber = function(num) {
    for (var i = 0; i < Math(num/2); i++) {
    	num[i]
    }
};